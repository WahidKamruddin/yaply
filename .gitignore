node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Local Netlify folder
.netlify

# Sister platform apps (each has its own GitHub repo)
yaply-ios/
yaply-android/
yaply-native/

# Local scratch output, not part of the app
brag-output/

# Supabase CLI local state (linked project, versions, local branch) — machine-specific
supabase/.temp/
supabase/.branches/

# Playwright e2e artifacts and minted local-stack sessions
/test-results/
/playwright-report/
/playwright/.cache/
/e2e/.auth/
